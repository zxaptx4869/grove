import { useState } from "react";
import { ActivityIndicator, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { router } from "expo-router";
import { useAuth } from "@/src/auth";
import { theme } from "@/src/theme";

export default function Login() { const { signIn } = useAuth(); const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const submit = async () => { setBusy(true); setError(""); try { await signIn(username, password, false); router.replace("/(tabs)"); } catch (e) { setError(e instanceof Error ? e.message : "登录失败，请重试"); } finally { setBusy(false); } }; return <View style={s.page}><Text style={s.mark}>G</Text><Text style={s.title}>登录 Grove</Text><Text style={s.copy}>使用 Grove 账号进入你的 Workspace。</Text><TextInput style={s.input} placeholder="账号" value={username} onChangeText={setUsername} autoCapitalize="none" /><TextInput style={s.input} placeholder="密码" value={password} onChangeText={setPassword} secureTextEntry /><Text style={s.error}>{error}</Text><Pressable style={s.primary} onPress={() => void submit()} disabled={busy}>{busy ? <ActivityIndicator color="#fff" /> : <Text style={s.primaryText}>登录</Text>}</Pressable><Pressable onPress={() => router.push("/register")}><Text style={s.link}>还没有账号？注册</Text></Pressable></View>; }
const s = StyleSheet.create({ page:{flex:1,backgroundColor:theme.bg,padding:24,justifyContent:"center"},mark:{backgroundColor:theme.green,color:"white",fontWeight:"800",fontSize:18,width:42,height:42,textAlign:"center",textAlignVertical:"center",borderRadius:10,marginBottom:24},title:{fontSize:25,fontWeight:"700",color:theme.ink},copy:{color:theme.muted,marginTop:8,marginBottom:28},input:{height:50,borderWidth:1,borderColor:theme.border,borderRadius:10,backgroundColor:theme.surface,paddingHorizontal:14,marginBottom:12,color:theme.ink},primary:{height:50,borderRadius:10,backgroundColor:theme.green,alignItems:"center",justifyContent:"center",marginTop:8},primaryText:{color:"white",fontWeight:"700"},link:{color:theme.green,textAlign:"center",fontWeight:"600",padding:18},error:{color:theme.error,minHeight:20,fontSize:13} });
