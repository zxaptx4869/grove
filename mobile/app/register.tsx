import { useState } from "react";
import { ActivityIndicator, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { router } from "expo-router";
import { useAuth } from "@/src/auth";
import { theme } from "@/src/theme";
export default function Register() { const { signIn } = useAuth(); const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const submit = async () => { setBusy(true); setError(""); try { await signIn(username, password, true); router.replace("/(tabs)"); } catch (e) { setError(e instanceof Error ? e.message : "注册失败，请重试"); } finally { setBusy(false); } }; return <View style={s.page}><Text style={s.title}>创建 Grove 账号</Text><Text style={s.copy}>注册后会创建一个独立的 Workspace。</Text><TextInput style={s.input} placeholder="账号（2–64 位字母、数字或下划线）" value={username} onChangeText={setUsername} autoCapitalize="none" /><TextInput style={s.input} placeholder="密码（至少 8 位）" value={password} onChangeText={setPassword} secureTextEntry /><Text style={s.error}>{error}</Text><Pressable style={s.primary} onPress={() => void submit()} disabled={busy}>{busy ? <ActivityIndicator color="#fff" /> : <Text style={s.primaryText}>注册并进入</Text>}</Pressable><Pressable onPress={() => router.back()}><Text style={s.link}>返回登录</Text></Pressable></View>; }
const s = StyleSheet.create({ page:{flex:1,backgroundColor:theme.bg,padding:24,justifyContent:"center"},title:{fontSize:25,fontWeight:"700",color:theme.ink},copy:{color:theme.muted,marginTop:8,marginBottom:28},input:{height:50,borderWidth:1,borderColor:theme.border,borderRadius:10,backgroundColor:theme.surface,paddingHorizontal:14,marginBottom:12,color:theme.ink},primary:{height:50,borderRadius:10,backgroundColor:theme.green,alignItems:"center",justifyContent:"center",marginTop:8},primaryText:{color:"white",fontWeight:"700"},link:{color:theme.green,textAlign:"center",fontWeight:"600",padding:18},error:{color:theme.error,minHeight:20,fontSize:13} });
