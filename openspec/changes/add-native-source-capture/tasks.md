## 1. 规划工件与依赖

- [x] 1.1 四类工件齐备并通过 `openspec validate --all --strict`。
- [x] 1.2 `npx expo install expo-image-picker expo-image-manipulator` 安装 SDK 57 匹配版本，`mobile/package.json` 记录依赖。

## 2. 采集逻辑模块

- [x] 2.1 `src/capture/batch.ts`：批次键派生（`{batchId}:{i}`，序号从 1）、提交单元派生（每张一条 / 合并一条 / 文本）、标题派生、汇总与进度文案。验收：单测覆盖重试复用键、新一轮换键、标题形态。
- [x] 2.2 `src/capture/submit.ts`：串行执行器，逐条上传并在成功后触发处理，部分失败继续、只重试失败项。验收：注入假上传器断言顺序、部分失败汇总文案、失败项重试。
- [x] 2.3 `src/capture/upload.ts`：multipart 上传通道（`files`/`capture_key`/可选字段、不设置 `Content-Type`、90 秒超时、`detail` 透传、200 与 201 都成功、不自动重试）。验收：fetch mock 断言字段、超时中止与错误文案。
- [x] 2.4 `src/capture/image.ts` 与 `src/capture/picker.ts`：按朝向压缩到长边 2048 并转 jpg、不放大、HEIC 转码；权限检查/申请、「去设置」跳转、相机单张与相册多选（`selectionLimit: 5`）。

## 3. 采集界面

- [x] 3.1 收集栏目替换占位：三入口横排（相机 / 相册 / 文本），相册下钻「每张一条 / 多张合并一条」。
- [x] 3.2 采集表单：补充说明 + 所属项目（默认未归属），图片不提供正文输入；缩略图可逐张移除、相机来源可重拍。
- [x] 3.3 结果反馈（全屏提交覆盖层）：进行中（含「正在上传 N 张中的第 M 张」）、全部成功、部分失败（「已提交 M 条，N−M 条未成功」+ 逐项重试）、全部失败、上传失败与处理启动失败分别提示、400 原文透传。
- [x] 3.4 权限被拒：用途说明 + 「去设置」入口，不反复弹窗。验收：组件测试断言文案与跳转动作。
- [x] 3.5 页面流转：入口页 / 采集页 / 权限页互斥，采集页顶栏返回并丢弃草稿，提交以覆盖底部导航的全屏覆盖层呈现，关闭后回到入口页并清空草稿、提交会话与批次键。验收：组件测试断言入口页不出现表单、采集页不出现入口行、提交出现覆盖层、关闭覆盖层后回到入口页且表单已清空。
- [x] 3.6 提交结束停留结果页：四种结果都停在覆盖层，底部固定区域（安全区之上、不随内容滚动）提供「回到收集」，进行中不提供该出口、返回键不关闭；处理未启动在卡片内提示。验收：组件测试断言全部成功不自动跳转且底部按钮可返回、400 仍在覆盖层显示原文、进行中无底部出口。
- [x] 3.7 真机验收反馈微调：提交进度压在材料缩略图上（压缩中 / 上传中 / 等待 / 已提交 / 未成功），缩略图固定用采集原图；底部出口去掉 1px 分隔线。验收：组件测试断言缩略图动效与等待状态。

## 4. 验证与收尾

- [x] 4.1 `cd mobile && npm run lint && npm run typecheck && npm test` 全部通过。
- [x] 4.2 用 `curl` 冒烟核对后端合同：multipart 提交 + `capture_key` 同键重复提交返回 200，确认移动端假设与实现一致。
- [x] 4.3 每段可验证修改做本地提交（中文 Conventional Commits），输出改动清单、规格条目、验证命令与结果、未验证项与真机走查清单；**不推送、不合并、不归档**，停下等人工验收。
- [x] 4.4 修真机「第二次采集」500（SQLite 写锁）：触发处理幂等兜底、写库与模型调用分段提交、SQLite 启用 WAL 与 15 秒写锁等待。验收：`cd backend && .venv/bin/python -m pytest tests/test_sqlite_concurrency.py` 全绿。

## 5. 待人工走查（用户执行）

- [x] 5.1 相册权限三态：全部允许、仅选择的照片（部分授权）、被拒后进入权限页并提供「去设置」（2026-09-24 用户真机走查通过）。
- [x] 5.2 相册多选上限：选满 5 张由系统选择器限制，界面明确提示，不静默丢图（2026-09-24 用户真机走查通过）。
- [x] 5.3 不回归：三入口采集页、提交覆盖层与底部「回到收集」、缩略图上的压缩与上传进度、连续两次采集不再出现 500（2026-09-24 用户真机走查通过）。
- [x] 5.4 Android 系统浮层：复现「白色圆角卡 + 蓝色进度条」并留截图，按 `design.md` 的平台已知边界记录，不作出厂验收要求（2026-09-24 用户真机走查通过）。
