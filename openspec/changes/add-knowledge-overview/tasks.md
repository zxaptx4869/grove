## 1. 视图与路由

- [ ] 1.1 `ProjectPage` 增加 `view=overview` 分支渲染知识全景视图；旧 `view=mindmap` 兼容重定向到 `view=overview&mode=mindmap`
- [ ] 1.2 `AppShell` 沉浸式条件覆盖 `view=overview`（隐藏侧栏与顶栏占位）
- [ ] 1.3 知识空间页头与项目首页的「思维导图」入口替换为「知识全景」（默认 `view=overview`）
- [ ] 1.4 URL `mode=sunburst|mindmap` 参数驱动模式切换与深链，刷新保持

## 2. 旭日图模式

- [ ] 2.1 从 git 历史恢复旭日图实现并组件化为 `SunburstPanel`：按深度单色扇区、宽度=合计数、hover 祖先高亮 + 「直接/后代/合计」悬浮窗、点击钻取 + 面包屑返回
- [ ] 2.2 标签策略：按弧长缩写，hover 补显完整名称
- [ ] 2.3 缩放与平移：SVG viewBox 动态化，滚轮以光标为中心、按钮放大/缩小/「适应窗口」，零依赖
- [ ] 2.4 可见扇区上限（建议 300）与超出提示「聚焦/钻取后查看」
- [ ] 2.5 空目录、加载中、加载失败重试等状态显式处理
- [ ] 2.6 为 `SunburstPanel` 补充组件测试：计数、钻取与返回、缩放、标签、空态

## 3. 大纲联动与详情

- [ ] 3.1 右侧目录大纲点击联动旭日图（选中并钻取）
- [ ] 3.2 直接知识列表配合 `EntryPopover`：hover 预览、点击固定、可关闭
- [ ] 3.3 大纲与知识面板可折叠，画布占满剩余空间
- [ ] 3.4 补充大纲联动与悬浮详情测试

## 4. 模式切换与跨模式联动

- [ ] 4.1 顶部「旭日图 | 思维导图」segmented 切换；两模式同时挂载、显隐切换保留各自状态
- [ ] 4.2 旭日图「在思维导图中查看」→ `mode=mindmap&node=<id>` 并聚焦该节点
- [ ] 4.3 思维导图极简阅读栏增加「查看全景」→ 切回旭日图模式并保持当前节点
- [ ] 4.4 补充模式切换与跨模式联动测试（含 URL 深链）

## 5. 验证与提交

- [ ] 5.1 全量回归：后端测试、前端 `npm run test:run`、`npm run build`；`npm run lint` 无本 change 新增问题
- [ ] 5.2 手工走查：知识全景入口、默认旭日图、切换模式、缩放/钻取、跨模式联动、返回知识空间定位
- [ ] 5.3 视觉验收：在 1280 / 1440 / 1600 对照静态原型检查旭日图与知识全景布局
- [ ] 5.4 `openspec validate --all --strict` 通过后归档 `add-knowledge-overview`，同步主规格
- [ ] 5.5 本地提交（Conventional Commits 中文信息）；不 push、不 merge，等待用户确认
