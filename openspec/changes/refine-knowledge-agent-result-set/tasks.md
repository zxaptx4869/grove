## 1. 相关性分层与授权集合

- [ ] 1.1 为语义召回结果增加候选角色和不可展示边界，并实现同一 Agent 使用的三类相关性选择工具。
- [ ] 1.2 只从直接相关项构造有序授权结果句柄，收紧列表输出、`read_entries`、Evidence 和位置指代到该集合。
- [ ] 1.3 更新定义型问题与知识库/来源/通用知识边界提示，确保空直接结果不以弱相关项补足。
- [ ] 1.4 补充直接优先、间接/不相关默认隐藏、集合一致、定义型问题和伪造句柄的后端离线测试。

## 2. 多轮单一工具编排与参数兼容

- [ ] 2.1 建立跨两个语义入口共享的规范化查询键和串行去重，保留成功查询供同轮与续聊复用。
- [ ] 2.2 为承接表达和搜索后读取失败保存最小续执行状态，只重试失败步骤。
- [ ] 2.3 兼容旧式 `sort` 对象和可空字段字符串 `"null"`，继续拒绝冲突、非法与越权参数。
- [ ] 2.4 补充上下文复用、单一搜索入口、成功步骤不重复、参数兼容及 Workspace/项目/目录归属回归测试。

## 3. 结果状态与工作台展示

- [ ] 3.1 增加未执行终态，并细分搜索成功但读取失败、预算耗尽、能力不支持和系统故障的原因码与确定性兜底。
- [ ] 3.2 更新实验工作台状态标签、缺口提示和直接相关列表元数据，确保原始候选不生成前端卡片。
- [ ] 3.3 补充参数错误不伪造部分完成、读取失败保留授权集合和前端状态表达测试。

## 4. 回归验证与交付

- [ ] 4.1 运行受影响后端测试、ruff、前端工作台测试、TypeScript、ESLint 和构建，并确认目录定位、叶子、统计、正文、候选稿与 Workspace 隔离回归通过。
- [ ] 4.2 运行 `openspec validate --all --strict`，记录自动化验证结果与真实模型未验证边界，并完成分段中文 Conventional Commits 本地提交。
- [ ] 4.3 准备工作台人工验收清单：定义型直接结果、间接默认隐藏、承接查询单入口、搜索成功读取失败、候选稿不写入。
- [ ] 4.4 用户在实验工作台完成真实模型人工验收并反馈通过。
- [ ] 4.5 经用户另行明确批准后同步规格并归档；本次实施不执行归档、推送或合并。
