## 1. 相关性分层与授权集合

- [x] 1.1 为语义召回结果增加候选角色和不可展示边界，并实现同一 Agent 使用的三类相关性选择工具。
- [x] 1.2 只从直接相关项构造有序授权结果句柄，收紧列表输出、`read_entries`、Evidence 和位置指代到该集合。
- [x] 1.3 更新定义型问题与知识库/来源/通用知识边界提示，确保空直接结果不以弱相关项补足。
- [x] 1.4 补充直接优先、间接/不相关默认隐藏、集合一致、定义型问题和伪造句柄的后端离线测试。

## 2. 多轮单一工具编排与参数兼容

- [x] 2.1 建立跨两个语义入口共享的规范化查询键和串行去重，保留成功查询供同轮与续聊复用。
- [x] 2.2 为承接表达和搜索后读取失败保存最小续执行状态，只重试失败步骤。
- [x] 2.3 兼容旧式 `sort` 对象和可空字段字符串 `"null"`，继续拒绝冲突、非法与越权参数。
- [x] 2.4 补充上下文复用、单一搜索入口、成功步骤不重复、参数兼容及 Workspace/项目/目录归属回归测试。

## 3. 结果状态与工作台展示

- [x] 3.1 增加未执行终态，并细分搜索成功但读取失败、预算耗尽、能力不支持和系统故障的原因码与确定性兜底。
- [x] 3.2 更新实验工作台状态标签、缺口提示和直接相关列表元数据，确保原始候选不生成前端卡片。
- [x] 3.3 补充参数错误不伪造部分完成、读取失败保留授权集合和前端状态表达测试。

## 4. 回归验证与交付

- [x] 4.1 运行受影响后端测试、ruff、前端工作台测试、TypeScript、ESLint 和构建，并确认目录定位、叶子、统计、正文、候选稿与 Workspace 隔离回归通过。
- [x] 4.2 运行 `openspec validate --all --strict`，记录自动化验证结果与真实模型未验证边界，并完成分段中文 Conventional Commits 本地提交。
- [x] 4.3 准备工作台人工验收清单：定义型直接结果、间接默认隐藏、承接查询单入口、搜索成功读取失败、候选稿不写入。
- [ ] 4.4 用户在实验工作台完成真实模型人工验收并反馈通过。
- [ ] 4.5 经用户另行明确批准后同步规格并归档；本次实施不执行归档、推送或合并。

## 自动化验证记录（2026-09-09）

- 业务代码修改前：`openspec validate --all --strict`，60 passed、0 failed。
- 后端：`backend/.venv/bin/ruff check backend` 通过；受影响的对话循环、工作台、共享只读工具、结构化查询、语义搜索、候选稿、API、续聊、结果协议和 Workspace 隔离共 291 个测试通过。
- 前端：工作台 9 个 Vitest 测试通过；`npm run build` 通过；`npm run lint` 为 0 error，保留 `DirectoryDraftDialog.tsx` 两条与本 change 无关的既有 Hook dependency warning。
- 收尾：`git diff --check` 通过；全库 OpenSpec 严格校验再次通过。
- 边界：以上 FunctionModel 与离线测试只证明工具合同、授权结果集合、参数兼容和状态边界，不证明真实模型对所有自然语言表达的语义理解已经通过。

## 工作台人工验收清单

- [ ] 输入“甲醛是什么”，确认先显示概念和核心结论，随后只列少量直接相关正式记录；知识库记录、Source 原文和模型通用知识标识清楚。
- [ ] 检查同一查询的执行记录与列表卡，确认间接相关和不相关记录默认不出现，序号仅来自直接相关授权集合；没有直接记录时明确提示不足。
- [ ] 在上一轮得到“可以继续查询”的建议后输入“好的，你帮我查一下”，确认复用上一轮主题和项目范围，`search_knowledge` / `query_entries` 只执行一个语义入口。
- [ ] 在可复现搜索成功但正文读取失败的环境中确认状态为“搜索成功但读取失败”，已确认直接相关列表仍显示；下一轮“继续”只重试读取，不重复搜索。
- [ ] 请求生成候选修改稿，确认回答明确“尚未写入知识库”，只输出供审核文本；刷新正式记录后内容没有被保存、覆盖或修改。
