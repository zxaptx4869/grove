## 1. 真实目录只读工具

- [x] 1.1 提取项目树一致的同级稳定排序与路径辅助，并实现 `list_project_directories` v1 共享只读处理器。
- [x] 1.2 在处理器中校验 owner/Workspace 成员、项目引用和父节点归属，返回项目不存在、歧义、越权、父节点错误与空目录的明确状态。
- [x] 1.3 将工具注册到现有只读 registry，并以真实 SQLite Node 数据测试根级/子级、空目录、同名目录、跨项目和跨 Workspace 边界。

## 2. 统一循环与历史接线

- [x] 2.1 暴露目录查询工具与项目真实 ID，增加目录专用结果句柄和按实际展示位置解析父节点的表面参数。
- [x] 2.2 扩展精简历史，仅保存目录项目、父节点、顺序、路径、数量和完整性，并保持 Entry 列表引用边界不变。
- [x] 2.3 验证目录工具继承动作预算、超时/取消、审计和 SQLite 事务串行，且失败后下一轮可继续。

## 3. 权威结果语义与工作台展示

- [x] 3.1 为统计、Entry 列表和目录列表生成查询对象、范围、筛选、分组、数量、完整性及中文显示名元数据。
- [x] 3.2 扩展统一块渲染与实验工作台目录结果视图，使用确定性标题和桶标签，忽略模型对结构化事实的错误命名。
- [x] 3.3 补充无模型前后端测试，覆盖 `main_type` 不得显示为目录统计、完整性/返回数量、目录追问及混合结果回归。

## 4. 验证、提交与人工验收准备

- [x] 4.1 运行相关后端测试、ruff、前端测试/lint/build，并修复本 change 引入的回归。
- [x] 4.2 完成全库 `openspec validate --all --strict` 收尾校验，记录真实命令与结果；不运行真实模型或历史消息重放。
- [x] 4.3 分段本地提交已验证成果，动态重启隔离实验工作台并确认地址可访问，交付人工测试问题与已知限制。

## 5. 统一部分完成与确定性收尾

- [x] 5.1 建立五类轮次终态和 StopState，归一预算、能力、拒绝、超时、收尾校验及系统异常，并保留准确停止原因和未完成步骤。
- [x] 5.2 实现只使用当前轮可信句柄的无模型兜底 blocks；覆盖 statistic、list/directories、evidence、insufficient，禁止虚假统计和历史引用。
- [x] 5.3 为递归/分页/批量任务增加剩余预算预检、重复动作保护和最小 continuation，支持明确继续或准确说明不可续。
- [x] 5.4 扩展现有目录工具以一次聚合真实整树叶子节点，沿用 Workspace、项目、审计、预算、超时和事务边界。
- [x] 5.5 更新工作台持久化合同、README 和页面状态展示，区分 partial_completed、unsupported、denied、failed，并保持未知工具调用数不显示为 0。

## 6. 第二批增量验证与交付

- [x] 6.1 用无真实模型测试预算耗尽、Entry/Evidence 预算、收尾无预算/非法/超时、能力不足、当前轮隔离、continuation 和后续普通问题。
- [x] 6.2 用真实 SQLite 测试叶子聚合、权限/空结果/数据库异常和事务后状态保存，并回归目录、统计、列表、来源及跨轮追问。
- [x] 6.3 执行相关后端/前端测试、ruff、Python 编译、前端构建、`git diff --check` 和全库 OpenSpec 严格校验，记录真实结果并本地提交。
- [x] 6.4 重启当前实验工作台并确认可访问；不发送真实模型请求，保留 change 待用户人工验收，不归档、不推送、不合并。

## 7. 名称与路径直接定位

- [x] 7.1 扩展目录工具 `find` 操作，支持项目内精确名称、必要的包含匹配和完整路径定位，返回真实 Node 身份、路径、深度、叶子确认、计数、歧义、截断与权限状态。
- [x] 7.2 将目录定位接入统一循环说明、权威语义、精简历史和结果句柄复用；已知名称优先直接定位，同名时展示候选路径。
- [x] 7.3 让现有 Entry 查询从目录结果句柄解析真实 Node，并按 direct/subtree 范围查询；共享层重新校验 Workspace、项目和 Node 归属。

## 8. 定位增量验证与交付

- [x] 8.1 用真实 SQLite 覆盖精确名称、完整路径、同项目同名、跨项目同名、不存在、项目不存在、无权限、路径/Node 越权、截断与定位后 Entry 查询。
- [x] 8.2 用无模型循环测试深层目录无需逐级遍历、叶子只由服务端确认、同名候选、后续句柄复用和预算不足部分完成，并回归已有目录、统计与 Workspace 隔离。
- [x] 8.3 更新工作台说明并执行相关后端/工作台测试、ruff、Python 编译、前端构建、`git diff --check` 与全库 OpenSpec 严格校验。
- [x] 8.4 本地提交已验证成果；不发送真实模型请求，不归档、不推送、不合并，交付人工测试清单。

验证记录（2026-09-08）：全量后端 pytest、全后端 ruff、`compileall`、前端 135 项测试、前端 lint/build、`git diff --check` 与全库 59 项 OpenSpec 严格校验通过；前端 lint 保留 2 条本 change 范围外的既有 Hook 依赖 warning。未运行付费评测或真实模型请求。

## 9. 人工验收缺陷修复

- [x] 9.1 修复目录完整未命中后模型回退根级遍历并因冗余参数被误判为权限拒绝的问题，以确定性空结果明确回答目录不存在。
- [x] 9.2 增加无真实模型回归测试，执行受影响后端检查、OpenSpec 严格校验并本地提交。

人工验收缺陷验证记录（2026-09-09）：新增无真实模型回归覆盖“墙纸施工”完整未命中、阻止根级及伪造父节点回退、确定性空结果文案、收尾软上限下直接渲染，并验证 dispatcher 将非法参数与权限拒绝分开；全量后端 pytest、相关目录/循环/工作台测试、全后端 ruff、`compileall`、`git diff --check` 与全库 59 项 OpenSpec 严格校验通过。未运行付费评测或真实模型请求。

## 10. 项目范围语义搜索收敛

- [x] 10.1 扩展对话表面 `search_knowledge` 的项目范围参数；明确项目名时转发到严格项目语义查询，保留 Workspace 隔离与审计/预算链路。
- [x] 10.2 修复通用语义搜索的候选合并：重排已有结果时只展示重排选中的真实 Entry，模型降级或无结果时才使用原始召回兜底。
- [x] 10.3 增加无真实模型回归测试，覆盖项目范围路由及无关候选不回填，并执行后端、工作台、静态检查与 OpenSpec 严格校验。

## 11. 语义结果完成状态修正

- [x] 11.1 在工具结果摘要中保留 `has_more`/截断信息，并让轮次状态只对确有后续结果的 limited 标记未完成。
- [x] 11.2 增加无真实模型回归测试，覆盖 `has_more=false` 的正常完成与 `has_more=true` 的部分完成。
- [x] 11.3 执行后端全量测试、静态检查、前端构建与 OpenSpec 严格校验，并本地提交。

## 12. 候选修改稿与正式写入分流

- [x] 12.1 增加候选稿与明确写入的确定性话术边界；自行审核/更新优先生成候选稿，明确保存、覆盖和写入仍返回 unsupported。
- [x] 12.2 为候选稿注入按需 instructions 并校验未写入声明、原记录、建议补充、候选版本和来源边界；误调 `report_unsupported` 时纠正而不记录虚假能力不足事件。
- [x] 12.3 增加无真实模型回归，覆盖候选表达、混合表达、直接写入、无写工具、无虚假 Evidence 和既有工作台行为。
- [x] 12.4 执行后端全量测试、工作台测试、ruff、Python 编译、`git diff --check`、前端构建和全库 OpenSpec 严格校验并本地提交。

候选修改稿缺陷验证记录（2026-09-09）：无真实模型测试覆盖 7 类候选表达、7 类明确写入表达、模型误调 unsupported 后纠正、候选稿五项语义校验、无写入工具与无虚假 Evidence，以及明确保存/覆盖继续 unsupported；相关循环/工作台/只读工具/结构化查询测试、后端全量 pytest、全后端 ruff、`compileall`、前端 135 项测试、前端 lint/build、`git diff --check` 与全库 59 项 OpenSpec 严格校验通过。前端 lint 保留 2 条本 change 范围外的既有 Hook 依赖 warning。未运行付费评测或真实模型请求，未修改正式 Entry、Source 或 Evidence。

## 13. 候选稿来源边界同义表达

- [x] 13.1 修复来源边界校验依赖固定文案的问题，接受“并非来自该来源原文”等等价表达，同时继续拒绝缺少来源边界的候选稿。
- [x] 13.2 使用最新人工会话中的真实措辞增加无模型回归，并执行后端、工作台、静态检查和 OpenSpec 严格校验。
