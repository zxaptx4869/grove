## 1. 真实目录只读工具

- [x] 1.1 提取项目树一致的同级稳定排序与路径辅助，并实现 `list_project_directories` v1 共享只读处理器。
- [x] 1.2 在处理器中校验 owner/Workspace 成员、项目引用和父节点归属，返回项目不存在、歧义、越权、父节点错误与空目录的明确状态。
- [x] 1.3 将工具注册到现有只读 registry，并以真实 SQLite Node 数据测试根级/子级、空目录、同名目录、跨项目和跨 Workspace 边界。

## 2. 统一循环与历史接线

- [x] 2.1 暴露目录查询工具与项目真实 ID，增加目录专用结果句柄和按实际展示位置解析父节点的表面参数。
- [x] 2.2 扩展精简历史，仅保存目录项目、父节点、顺序、路径、数量和完整性，并保持 Entry 列表引用边界不变。
- [x] 2.3 验证目录工具继承动作预算、超时/取消、审计和 SQLite 事务串行，且失败后下一轮可继续。

## 3. 权威结果语义与工作台展示

- [x] 3.1 为统计、Entry 列表和目录列表生成查询对象、范围、筛选、分组、数量、完整性及中文显示名元数据。
- [x] 3.2 扩展统一块渲染与实验工作台目录结果视图，使用确定性标题和桶标签，忽略模型对结构化事实的错误命名。
- [x] 3.3 补充无模型前后端测试，覆盖 `main_type` 不得显示为目录统计、完整性/返回数量、目录追问及混合结果回归。

## 4. 验证、提交与人工验收准备

- [x] 4.1 运行相关后端测试、ruff、前端测试/lint/build，并修复本 change 引入的回归。
- [x] 4.2 完成全库 `openspec validate --all --strict` 收尾校验，记录真实命令与结果；不运行真实模型或历史消息重放。
- [x] 4.3 分段本地提交已验证成果，动态重启隔离实验工作台并确认地址可访问，交付人工测试问题与已知限制。

## 5. 统一部分完成与确定性收尾

- [ ] 5.1 建立五类轮次终态和 StopState，归一预算、能力、拒绝、超时、收尾校验及系统异常，并保留准确停止原因和未完成步骤。
- [ ] 5.2 实现只使用当前轮可信句柄的无模型兜底 blocks；覆盖 statistic、list/directories、evidence、insufficient，禁止虚假统计和历史引用。
- [ ] 5.3 为递归/分页/批量任务增加剩余预算预检、重复动作保护和最小 continuation，支持明确继续或准确说明不可续。
- [ ] 5.4 扩展现有目录工具以一次聚合真实整树叶子节点，沿用 Workspace、项目、审计、预算、超时和事务边界。
- [ ] 5.5 更新工作台持久化合同、README 和页面状态展示，区分 partial_completed、unsupported、denied、failed，并保持未知工具调用数不显示为 0。

## 6. 第二批增量验证与交付

- [ ] 6.1 用无真实模型测试预算耗尽、Entry/Evidence 预算、收尾无预算/非法/超时、能力不足、当前轮隔离、continuation 和后续普通问题。
- [ ] 6.2 用真实 SQLite 测试叶子聚合、权限/空结果/数据库异常和事务后状态保存，并回归目录、统计、列表、来源及跨轮追问。
- [ ] 6.3 执行相关后端/前端测试、ruff、Python 编译、前端构建、`git diff --check` 和全库 OpenSpec 严格校验，记录真实结果并本地提交。
- [ ] 6.4 重启当前实验工作台并确认可访问；不发送真实模型请求，保留 change 待用户人工验收，不归档、不推送、不合并。
