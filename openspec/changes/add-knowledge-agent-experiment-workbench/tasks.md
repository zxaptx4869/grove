## 1. 本地隔离服务

- [x] 1.1 实现父进程启动器、loopback 限制、钥匙串读取、一致快照、子进程环境绑定、Worker 关闭、启动预检和退出原库指纹复核；启动本身模型调用为 0。
- [x] 1.2 实现 HttpOnly 本机会话、服务端 demo/Workspace 绑定、会话与轮次归属校验，以及当前运行和历史只读记录的 700/600 权限原子持久化。

## 2. 统一循环会话

- [x] 2.1 以服务级共享预算封装已有 `build_agent`、`LoopState`、`run_turn` 和实验副本 Run 生命周期，支持任意轮次、新建对话、请求去重和全局串行。
- [x] 2.2 实现真实阶段、轮次轮询、用户取消、失败／材料不足／收尾状态和已验证结果保留；验证取消不派发额外模型调用、预算不因刷新或新建对话重置。
- [x] 2.3 实现每轮反馈保存与更新、脱敏 JSON 导出和明确本地路径，不保存密码、密钥、Cookie 或隐藏推理。

## 3. 桌面实验工作台

- [x] 3.1 增加独立 Vite 多页入口及三栏工作台，实现元数据、会话列表、自由输入、新建、停止、防重复提交、刷新恢复和服务重启只读提示。
- [x] 3.2 实现文字、可信统计、有序列表、来源展开、折叠执行记录、模型／token／估算／预算、失败和收尾展示，以及三类反馈和导出。
- [x] 3.3 按 design 视觉基线完成 1440px 主视口、1280/1600px 扩展视口和 1023/1024px 边界，核对截图、计算样式、键盘、焦点、溢出及适用状态。

## 4. 验证与交付

- [x] 4.1 增加后端无模型测试，覆盖隔离、认证、归属、持久化、连续对话、来源、诊断、反馈、导出、取消、失败、预算耗尽和重启只读；不调用真实模型。
- [x] 4.2 增加前端组件测试并通过相关 Vitest、ESLint、TypeScript/Vite build、后端 pytest/ruff/compile 和全库 OpenSpec 严格校验。
- [x] 4.3 使用显式离线桩启动完整服务并完成桌面浏览器交互与截图验收；随后启动真实本地服务但不主动发送消息，交付 URL、数据说明、反馈路径、已知限制和停止方式，等待用户试用。

## 验证记录（2026-09-07）

- 后端无模型回归：`tests/test_dialogue_workbench.py` 与
  `tests/test_knowledge_agent_dialogue_loop.py` 合计 60 项通过；覆盖连续轮次、请求去重、列表顺序、
  来源、公开诊断、反馈、导出、Cookie、身份字段拒绝、同源限制、取消、Provider 失败、预算耗尽、
  隔离指纹异常和重启只读。取消用例确认没有文本请求和独立收尾。
- 前端组件：工作台 4 项 Vitest 通过；TypeScript/Vite 生产构建通过；全库 ESLint 0 error，保留
  两项既有 `DirectoryDraftDialog.tsx` Hook dependency warning，本 change 未修改该文件。
- Python：相关 ruff 与 compileall 通过；`git diff --check` 通过。
- 完整离线服务通过浏览器验收：实际发送连续消息，渲染可信统计、两个有序 Entry、可展开 Evidence、
  两个工具及一次模型调用的公开诊断；反馈刷新后仍存在，服务重启后旧会话只读。慢查询取消显示
  0 个工具、0 次模型调用且未收尾；显式 Provider 失败保留原因；导出端点返回脱敏附件。
- 响应式计算样式：1280px 为 `260/680/340`，1440px 为 `260/840/340`，1600px 为
  `260/1000/340`，均无横向溢出；1024px 显示三栏，1023px 只显示电脑访问提示。
- 离线启动初始预算为文本 0/192、向量 0/64，确认启动不调用模型；停止脚本可终止父进程并由父进程
  转发到子服务，退出后原库指纹复核通过。
- 真实服务只完成启动预检，没有发送用户消息。页面显示 `deepseek/deepseek-chat`、当前快照、
  文本 0/192、向量 0/64 和隔离环境就绪；服务留在 `http://127.0.0.1:8765/workbench` 等待用户试用。
