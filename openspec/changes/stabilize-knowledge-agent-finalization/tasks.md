## 1. 历史与输入门禁

- [x] 1.1 将每轮历史改为从结构化回答块、工具事件和完成状态生成，排除 Entry 正文与 Evidence 原文，并保留结论、建议、决定、授权顺序和未完成步骤
- [x] 1.2 实现单轮 1200 字符回答摘要与整体 5000 token 目标的确定性分级压缩，必要上下文仍超过硬上限时保留准确停止状态
- [x] 1.3 调整软阈值策略，使本轮第一次 9000–12000 正常请求可派发、后续请求达到 9000 时无条件进入收尾，并保持全部冻结预算数值不变

## 2. 收尾隔离与续执行

- [x] 2.1 建立不注册任何资料工具的独立 finalizer，共用既有 `DialogueAnswer`、授权引用与一次收尾请求边界
- [x] 2.2 检测并拒绝 finalizer 的资料工具调用，记录 `finalize_tool_attempted`，确保工具执行为零且不追加模型请求
- [x] 2.3 保存包含问题、指代、授权范围、可恢复材料和指纹的 finalize-only continuation，并实现 Workspace、对象、来源关系与材料有效性复验
- [x] 2.4 实现“继续”只调用 finalizer、成功后清理、主题切换清理和材料失效转重新核验，不重复成功搜索或读取
- [x] 2.5 将 `model_only` 回答依据传入 finalizer 和输出校验，过滤历史资料工具、授权集合与 Entry/Evidence 引用
- [x] 2.6 为无资料最终回答失败保存最小 answer-only continuation，支持“继续”和“下一轮继续”只重试回答
- [x] 2.7 识别带对象插入的“修改动作 + 交付/审核信号”候选稿请求，并保持直接保存、覆盖与写入仍不支持
- [x] 2.8 为 DeepSeek V4 普通求解与 finalizer 显式关闭思考模式，并保持模型名与全部冻结预算不变
- [x] 2.9 将 `can_continue` 收敛为真实 continuation，拦截无 continuation 的“继续”且不调用模型或资料工具
- [x] 2.10 兼容 text 块无害备注而继续拒绝错误句柄与越权引用，实现只输出内容的精简候选稿且不展开 Evidence
- [x] 2.11 在 finalizer 响应边界受限转换已知 `answer_summary + completion` 外壳，忽略模型完成声明并复用既有引用与 Workspace 校验
- [x] 2.12 将旧外壳转换前移为普通求解与 finalizer 共用的响应边界，在格式重试前恢复完整最终回答并保留原始响应、引用声明和兼容事件
- [x] 2.13 放宽候选修改稿的确定性校验，仅保留未写入、原记录/新增建议区分和来源边界硬条件；保存正常阶段候选文本，finalize 与 continuation 优先最小整理原稿
- [x] 2.14 将候选稿 finalizer 收敛为纯文本输出，校验前丢弃多余资料块；续执行保存完整稿件、缺口和关系指纹，复验时不重新授权历史句柄

## 3. 工作台状态与离线回归

- [x] 3.1 更新工作台公开完成状态和前端文案，准确区分来源已取得但分析未完成、非法工具尝试、输出失败、超时、系统故障与材料失效
- [x] 3.2 补充四轮现场、历史压缩、软硬阈值、无工具 finalizer、续执行保存/恢复/清理/失效及预算常量测试
- [x] 3.3 运行第三批授权集合、搜索去重、位置指代、Workspace/目录/统计/正文/候选稿只读回归，修复本 change 引入的问题
- [x] 3.4 补充离线回归，验证无资料收尾不引用历史 Evidence、失败可续执行且不重复资料读取；工作台真实现场仍待用户验收
- [x] 3.5 补充真实失败措辞、等价候选表达与带交付要求的直接写入负例离线回归
- [x] 3.6 补充 V4 非思考设置、三类真实收尾非法输出、continuation 真值、孤立继续零调用及精简候选稿渲染回归
- [x] 3.7 使用最新 F4 星真实载荷覆盖旧外壳成功转换、伪造或历史引用拒绝、未知结构拒绝、单模型请求与 finalizer 零资料工具执行
- [x] 3.8 使用最新候选稿真实载荷覆盖普通 Agent 单请求转换、候选只读边界、引用关系复验、临时声明清理和 finalizer 兼容回归
- [x] 3.9 补充自然段候选边界、有效候选稿保留、finalize 资料工具零执行和候选稿专用 continuation 回归
- [x] 3.10 使用 `session-20260911-212827` 第四轮形态回归完整候选稿附带无效 Evidence，验证纯文本完成、全文续执行恢复、零重复资料工具和成功清理

## 4. 验证与交付

- [x] 4.1 在业务代码修改前运行 `openspec validate --all --strict` 并修复工件问题（61 项通过）
- [x] 4.2 运行受影响后端测试与 Ruff、实验工作台前端测试/lint/build、`git diff --check` 和收尾全库 OpenSpec 严格校验
- [x] 4.3 将实现与真实自动化结果记录到本 change，按可验证阶段完成中文 Conventional Commits 本地提交
- [ ] 4.4 由用户在实验工作台重走甲醛四轮对话及失败后“继续”，核对序号、可信度分析、工具隔离、材料复用、相关性隐藏与候选稿只读
- [ ] 4.5 用户明确批准后再同步规格并归档；本任务不得执行归档、推送或合并

## 自动化验证记录（2026-09-10）

- 业务代码修改前：`openspec validate --all --strict` 通过，61 passed、0 failed。
- 后端：`backend/.venv/bin/ruff check backend` 通过；对话循环、工作台、共享只读工具、结构化查询、Entry 搜索、候选稿、API、续聊、结果协议、目录/统计/正文与 Workspace 隔离共 298 个测试通过。
- Python 静态编译：`backend/.venv/bin/python -m compileall -q app evals tests` 通过。
- 前端：工作台 10 个 Vitest 测试通过；`npm run build` 通过；`npm run lint` 为 0 error，保留 `DirectoryDraftDialog.tsx` 两条与本 change 无关的既有 Hook dependency warning。
- 收尾：`git diff --check` 通过；全库 `openspec validate --all --strict` 再次通过，61 passed、0 failed。
- 预算：离线测试断言软阈值 9000、硬上限 12000、文本请求 12、工具 8、Entry 30、Evidence 20、单轮 120 秒、批次文本 192 和向量 64 均未提高。
- 边界：以上 FunctionModel 与离线回归只证明历史、工具、授权、续执行和状态合同，不证明真实模型对自然语言指代及可信度回答质量已经通过；未运行真实模型评测，也未勾选人工验收。
- 本次缺口回归（2026-09-10）：新增 `model_only` finalizer 历史过滤、非法历史 Evidence 拒绝、answer-only continuation 保存/快照脱敏、“下一轮继续”只重试回答和零资料工具调用测试；定向对话循环测试通过。
- 前端提示更新为同时接受“继续”和“下一轮继续”；工作台 Vitest 139 个测试、TypeScript/Vite build 通过，ESLint 0 error，保留 `DirectoryDraftDialog.tsx` 两条既有 Hook dependency warning。
- 后端受影响测试单独运行通过；全文件串行运行时，既有 `_seed_finalize_material` 使用 `id(state)` 生成用户名，在长测试进程中可能因 Python 对象地址复用触发 SQLite 唯一键冲突，单测重跑通过，未将该测试夹具问题归入本次业务回归。
- 候选稿交付回归（2026-09-11）：真实失败原句“帮我把第一条的知识补充一下，发给我”、两类等价组合表达和带交付/审核要求的直接写入负例共 26 个定向用例通过；对话循环与模型设置共收集 133 个用例，整组运行时再次命中上述既有夹具唯一键冲突，其余 132 个通过，失败用例与模型设置 9 项随后单独重跑共 10 个通过。后端 Ruff、静态编译、前端生产构建及工作台 HTTP 200 启动检查通过；未发起真实模型请求，人工验收仍未勾选。
- V4 收尾稳定性回归（2026-09-11）：依据 `session-20260911-104336` / `fe11f237-c328-439e-8969-86be8ce2d517` 的三个失败轮次，补充 V4 非思考请求设置、finalizer 错误顶层结构、Entry/Evidence 句柄混用、text 块无害 `note`、continuation 真值、孤立“继续”零调用及候选正文精简交付测试。修复夹具唯一键后，对话循环 132 个测试全部通过；工作台与 AI 设置 20 个测试、前端 139 个测试通过，后端 Ruff 与静态编译通过，前端 lint 为 0 error 并保留两条既有 warning。所有检查均为离线合同和执行边界验证，未发起真实模型请求；V4 实际 reasoning tokens、自然语言理解和答案简洁度仍待工作台人工验收。
- 收尾旧外壳兼容回归（2026-09-11）：依据 `session-20260911-115702` / `7fae950d-fc2a-4e8c-8cb2-3d4190ad9a91` 第四轮真实响应，受限转换 `answer_summary + completion` 为标准 text/Evidence 块，丢弃模型完成声明并记录 `legacy_answer_summary_envelope` 兼容事件。成功转换、来源关系不一致和未知顶层结构共 3 类新增用例通过；对话循环 135 个测试全部通过，确认单模型请求且 finalizer 资料工具新增执行为零。工作台与 AI 设置 20 个测试、前端 139 个测试、后端 Ruff 与静态编译通过；前端 lint 0 error，保留两条既有 warning。未发起真实模型请求，实际 V4 收尾效果仍待人工验收。
- 普通求解统一兼容回归（2026-09-11）：依据 `session-20260911-123115` / `f614baa1-3ac3-422f-9e2e-3b254b3769b3` 第五轮候选稿，发现旧式 `answer_summary + completion` 兼容仅放在 finalizer 事后，导致完整候选稿先被格式校验拒绝、再丢失给 finalizer。现已将精确转换前移到普通求解与 finalizer 共用的 `BudgetedModel` 响应边界，保留原始公开响应、引用声明和兼容事件；普通 Agent 单请求候选稿、非法关系、未知结构、临时声明清理和 finalizer 零资料工具回归通过。对话循环 136 个测试、工作台与 AI 设置 20 个测试、前端 139 个测试、后端 Ruff 与静态编译通过；前端 lint 0 error，保留两条既有 warning。未发起真实模型请求，V4 实际自然语言效果仍待人工验收。
- 候选来源边界措辞回归（2026-09-11）：依据 `session-20260911-174737` / `86579fd8-88e9-434d-a6ff-b8a35175e4e4` 第五轮，确认旧外壳已成功转换，真正失败点是“没有对应的知识库来源原文”未被候选稿边界校验识别，导致无谓重试并进入 finalizer。补充“没有对应来源原文、没有来源原文、未提供来源原文、未取得来源原文”等确定性等价表达，普通 Agent 单请求候选稿回归和完整对话循环 136 个测试通过；未发起真实模型请求。
- 候选稿保留与专用续执行回归（2026-09-11）：候选校验改为硬边界与自然表达兼容，不再要求固定章节标题；正常阶段已生成的候选文本在 finalize 非法工具尝试后保留并进入 `candidate_draft` continuation，收尾与“继续”均不执行资料工具、不引入无效 Evidence，仅重试候选稿整理。自然段表达、写入声明拒绝、候选稿保留和只重试候选稿共 6 个定向用例及完整对话循环通过；未发起真实模型请求。
- 候选稿纯文本收尾回归（2026-09-11）：依据 `session-20260911-212827` 第四轮失败形态，候选 finalizer 现在于校验前确定性丢弃多余 Evidence/list/statistic/insufficient/Entry 块，保留合法 text 并记录 `candidate_text_only` 兼容事件。`candidate_draft` continuation 私有保存完整稿件、校验缺口和 Entry/Source 指纹，公开快照只返回文本长度与缺口摘要；“继续”只将原问题和完整稿件交给 finalizer，历史 Evidence 仅供程序复验且不重新授权。完整对话循环 142 项、工作台/共享查询/目录统计/正文/Workspace 相关后端 106 项、前端 139 项均通过；Ruff、Python 静态编译和前端生产构建通过，ESLint 0 error 并保留 2 条既有 warning。未发起真实模型请求，自然语言效果仍待工作台人工验收。

## 工作台人工验收清单

- [ ] 重启实验工作台后依次输入“甲醛是什么”→“好的，帮我解释这些等级标准的具体内容”→“第二个等级信息可信吗”→“那第一条呢，可信吗”。
- [ ] 核对授权列表顺序保持稳定，“第一条”仍对应国标 ENF；回答说明来源性质、可支持范围与缺少的外部核验，不把已有来源说成已完成官方交叉验证。
- [ ] 检查第四轮首次完整请求处于 9000–12000 时仍可派发；进入 finalize 后不再执行 `read_evidence`、搜索、目录或正文读取工具。
- [ ] 人为复现 Evidence 已取得但最终回答输出非法或超时，确认状态显示“来源已取得，可信度分析尚未完成”，且确实提供可用的“继续”。
- [ ] 输入“继续”，确认只重试最终回答，不重复成功搜索、Entry 或 Evidence 读取；完成后再次“继续”不会重复旧任务，材料失效时明确转为重新核验。
- [ ] 核对间接和不相关记录仍不展示；请求候选修改稿时仍只输出“尚未写入”的文本，不修改正式 Entry。
- [ ] 在第五轮输入“按你的分析，帮我把第一条的知识补充一下，发给我”，确认返回包含原记录要点、建议补充、修改后候选版本与来源边界的候选文本，并明确尚未写入。
- [ ] 新建 V4 Flash 会话核对不再产生 reasoning tokens；连续对话进入 finalizer 时应在本轮完成，若输出非法则只有真实可恢复材料时才显示“继续”。
- [ ] 候选稿完成后输入“就输出你补充后的知识内容就行，其他的都不用”，确认只显示候选正文与必要边界，不展开 Evidence；随后无待办时单独输入“继续”，确认零模型、零资料工具且不重复读取。

## 5. 第四阶段稳定性优化（2026-09-12）

- [x] 5.1 将普通 Entry/追问与来源核验意图分开，普通路径跳过 `read_evidence`，明确来源、原文、可信度或冲突时才读取 Evidence
- [x] 5.2 确保工具完成后只进入无工具 finalizer，预算边界停止新增资料动作并保留已确认结果，准确区分三类终态
- [x] 5.3 增加离线确定性回归：Evidence 按需门控、工具隔离、continuation 只重组回答、预算边界与 Workspace/Entry/Source 约束
- [x] 5.4 运行受影响后端测试、ruff、git diff 检查和 OpenSpec 严格校验，完成本地中文 Conventional Commit；保留人工真实对话验收和归档待用户处理

### 第四阶段验证记录（2026-09-12）

- `openspec validate --all --strict`：61 passed、0 failed。
- `backend/.venv/bin/ruff check evals/dialogue_loop/loop.py tests/test_knowledge_agent_dialogue_loop.py`：通过。
- `backend/.venv/bin/pytest -q tests/test_knowledge_agent_dialogue_loop.py tests/test_dialogue_workbench.py`：155 passed。
- 新增回归覆盖 Evidence 意图门控及 `evidence_not_required` 不触发不完整终态；未发起真实模型评测，人工工作台验收、归档、推送和合并仍待用户处理。
