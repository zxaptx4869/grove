## 1. 骨架确认

- [x] 1.1 确认非空知识空间页头与目录树栏的重复根节点入口，保留树栏 `+` 和空目录文字入口；运行 `openspec validate --all --strict` 验证变更工件。

## 2. 实现

- [x] 2.1 移除 `ProjectPage` 非空知识空间页头的“根节点”主按钮，不改根节点表单和树栏创建入口。
- [x] 2.2 更新 `ProjectPage` 测试，验证页头不再存在“根节点”按钮且“创建根节点”图标按钮仍可用；运行对应测试文件。

## 3. 验证

- [x] 3.1 运行前端测试、lint、build 和 `git diff --check`。
- [x] 3.2 在 1440×900 检查页头与目录树标题区，确认按钮层级、布局和可访问名称正确，并保存截图证据。
- [x] 3.3 同步主规格、归档 Change 并创建中文 Conventional Commits 本地提交；不 push、不 merge。
