# 归档与合并收尾记录

日期：2026-09-20。

## 授权及验收边界

用户经过多轮真机试用后，明确要求暂停知识详情弹层轻微抖动的修复，并授权核对无共享 Agent/Web 改动后归档，随后明确要求“合并推送”。本 change 按带已知遗留的例外关闭，不代表全部设备验收通过。任务 5.2 保持未勾选，不能将用户授权收尾替代逐项设备验证。

已知限制：

- 知识详情入场仍有轻微抖动，用户明确暂缓；不再追加修复或宣称消失。
- 发送消息闪动、键盘遮挡最新消息的修复提交 `10f4869` 已由 `e2e6b9c` 回退。当前没有这些缺陷已经修好的证据，本轮合并不重新实施该尝试。
- 全部字号、读屏、系统键盘、安全区和进程生命周期组合没有逐项真机通过证据。
- 本次收尾未操作设备、调用模型、启动服务或部署；自动化不能代替设备视觉和真实 Agent 语义验收。

## 规格同步纠正

首次归档为通过旧场景保留检查，机械补回了旧模式、旧引用条和默认最近对话场景，造成主规格新旧合同冲突；还发生需求标题拼接和错误的全量设备验收勾选。合并前已纠正，业务实现未因此变化。

纠正以归档前 `e2e6b9c` 中的正式计划与用户最终决定为准：对已经替换的行为明确使用 REMOVED/ADDED 描述；对继续适用的消息分页、范围隔离、前后台恢复等保留原有约束；新增要求放入 ADDED，不借用无关旧标题；已移除的草稿/修订能力仅保留只读边界要求。同步更新归档增量与主规格，去除错误拼接、废弃行为和误附到新需求下的旧场景。

主规格是目标合同，已知设备缺陷属于尚未完全满足目标的遗留，不为收尾降低产品要求。

## 验证证据

- 实现基线 `e2e6b9c`；本次规格纠正未修改移动端业务代码。
- 2026-09-20 合并前重新执行：移动端 17 suites / 78 tests 通过，typecheck、lint 通过。
- 既有 React Query 测试 `act` 警告保留，未通过屏蔽日志伪造无告警状态。
- 后端 `backend/`、Web `frontend/` 及共享 Agent 实现相对合并基线 `3b29e26` 无差异。
- 全库 OpenSpec 严格校验：63 项通过；差异格式检查通过。
- 在独立临时目录中，以 `3b29e26` 的五份主规格和本次纠正后的归档增量重新运行标准 `openspec archive`，五份重建需求块与最终主规格一致，未使用跳过同步/验证参数。REMOVED 区块缺少场景及增量数量较多的 CLI 提示为非阻断警告；任务 5.2 未完成提示与本次例外收尾一致。

本次授权包含合并、推送；远端特性分支保留，不删除。
