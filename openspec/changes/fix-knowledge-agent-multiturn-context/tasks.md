> 2026-09-06 状态：用户已确认任务状态修订，版本化状态与增量规划已接入。确定性回归通过，真实新旧对照正在执行并修复发现的问题；3.3、3.4 仍等待本修订验证和用户原生验收。

## 1. 前置与上下文

- [x] 1.1 四类工件齐备并通过 `openspec validate --all --strict`。
- [x] 1.2 实现可信范围与有界同话题历史装配、统计主题承接、澄清恢复及决策持久化；验证范围切换、新话题、恢复与隔离。
- [x] 1.3 实现最近展示列表的序号解析与本轮对象/证据复验；验证缺失、删除与越界对象不替换。

## 2. 依据与查询

- [x] 2.1 修复通用知识限制误判和受限回退；验证不查 Grove、仅用 Grove 及连续讨论。
- [x] 2.2 增加受限项目名称子集和项目分组；验证精确计数、零条项目、重名/越界拒绝、预算与 SQLite/MySQL 语义。
- [x] 2.3 明确全部知识类型口径，打通 B1、复合执行、共享图与结果协议；验证旧快照兼容。
- [x] 2.4 原生端适配项目桶名称和筛选范围，执行相关测试、typecheck、lint。

## 3. 验证与收尾

- [x] 3.1 执行相关后端测试与 ruff，按受影响核心边界扩大必要回归；端点 curl 确认已注册。
- [x] 3.2 demo 真实多轮基线复跑并核对结果、依据、范围和业务数据哈希；人工复核开放解释和对象指代，记录失败及修复后的结果。结果见 `docs/验收记录/knowledge-agent-multiturn-fix-2026-09-06.md`，严格评分保留 2 个正常截断标记及 10 个已阅读复核轮次。
- [ ] 3.3 保存已验证本地提交，提供原生端手动走查清单，等待用户实际反馈。
- [ ] 3.4 完成后全库 OpenSpec 严格校验，展示同步差异及归档目标；取得覆盖本次归档的批准后归档并收尾提交。

## 4. 对话任务状态修订

- [x] 4.1 完成最小任务状态设计，核对 proposal、specs、design、tasks 一致性和严格校验，提交本地规划工件，交付整体方案供确认；本轮不编辑业务代码。
- [x] 4.2 确认修订设计后实现版本化任务状态与通用归并：有界任务选择、条件来源、深入/恢复、硬边界、未决澄清，以及候选与有效状态分开提交；覆盖旧历史、恢复、取消和并发隔离。
- [x] 4.3 调整上下文与查询规划输入，保留原始消息，核实有界项目名称候选；查询增量编译为已有计划，结果路由不改筛选，复用统计工具与结果协议。
- [x] 4.4 接入开放讨论和复合/兼容分支的选定任务、用户陈述及依据限制；保持对象引用和当前 Evidence 边界，禁止新协议失败后静默放宽条件。
- [x] 4.5 按设计中的状态变化、对照数据与边界执行后端测试及相关调用方检查；确认实际统计口径在既有结果摘要和原生展示中一致。后端 604 项、原生相关 29 项测试通过，typecheck/lint/ruff 通过；设备实际展示仍由 3.3 用户验收。
- [ ] 4.6 固定调试集、保留集和评分口径，在相同模型/数据基线上运行 demo 新旧对照，保留用户五轮原话；分别报告条件错误、澄清、纠正恢复、完整对话成功率、调用数和耗时，不沿用先前评测作为本修订证据。
- [ ] 4.7 保存实现与自动化验证的本地阶段提交，更新用户走查清单和真实验收记录，再回到 3.3、3.4 完成验收与归档；不提前推送或合并。
