# 知林 Grove 产品蓝图

> 状态：V1 产品蓝图，2026-08-13 拆分为渐进式文档结构。本文是产品索引，不承载全部设计细节。

## 产品基线

知林 Grove 是一个面向个人项目的 AI 知识共创工具：把用户散落在截图、文字、网页和文档中的信息，整理成可确认、可追溯、符合个人理解方式的知识库，并逐步帮助用户发现知识缺口。

第一阶段只承诺一件事：用户把零散材料交给 Grove，AI 提取、拆分、推荐项目与目录，人以较低成本确认后，得到能继续浏览、检索和使用的正式知识。

当前优先完成 P0-A「可信整理闭环」和 P0-B「整理效率闭环」。目录共创与知识回忆属于下一阶段；主动发现是远期能力，不进入当前 MVP。

## 产品铁律

1. **AI 输出永远是候选。** AI 不得直接写入或覆盖正式 Entry。
2. **正式记录必须可溯源。** 每条 Entry 必须能追溯到 Source。
3. **数据按 Workspace 隔离。** 任何跨 Workspace 的读写都是缺陷。
4. **人在环上。** 确认、拒绝和修改是闭环的一部分，人拥有最终决定权。

## 阅读规则

1. 工程任务遵循 `AGENTS.md`；涉及产品范围与优先级时再读本索引，环境操作和普通排查不例行加载产品文档。
2. 根据下方路由，通常只读取与当前任务直接相关的 1 至 2 份专题；已读且未变化的内容直接复用。
3. 按当前任务读取相关主规格、change 段落和代码；首次建立范围与关键约束，发现关联影响或冲突再扩大范围，不默认通读全部工件。
4. 只有修改跨领域产品定义、审计整份蓝图或调整文档结构时，才通读全部专题。
5. 具体决策只在权威专题中维护；本索引只保存稳定摘要和路由，不复制详细规则。
6. `openspec/changes/archive/` 是历史记录，不作为当前产品范围入口。

## 专题文档

| 专题 | 权威范围 |
|---|---|
| [产品定位与阶段](产品蓝图/产品定位与阶段.md) | 产品定义、用户、核心场景、阶段承诺与原则 |
| [核心对象与关系](产品蓝图/核心对象与关系.md) | Workspace、Project、Source、Task、Extraction、Candidate、Entry、Directory Node |
| [采集整理与确认](产品蓝图/采集整理与确认.md) | 候选生成、质量判断、项目与目录推荐、确认台、关系判断、隐私与删除 |
| [目录与知识空间](产品蓝图/目录与知识空间.md) | 目录共创、知识浏览、卡片与列表、思维导图、搜索、AI 阅读与页面信息架构 |
| [Agent 架构与 AI 边界](产品蓝图/Agent架构与AI边界.md) | PydanticAI、Agent 分工、公共上下文与 Project Context Snapshot |
| [技术与端侧边界](产品蓝图/技术与端侧边界.md) | 技术栈、模型策略、基础设施、桌面 Web 与小屏边界 |
| [功能优先级与 Change 顺序](产品蓝图/功能优先级与Change顺序.md) | P0–P3、非目标、验证指标、OpenSpec change 顺序与待验证问题 |

## 任务路由

| 当前任务 | 优先读取 |
|---|---|
| 产品定位、目标用户、阶段范围 | 产品定位与阶段；功能优先级与 Change 顺序 |
| 项目生命周期、领域建模、数据库关系 | 核心对象与关系；功能优先级与 Change 顺序 |
| 采集、解析、任务状态、候选抽取 | 核心对象与关系；采集整理与确认 |
| 确认台、批量确认、项目或目录推荐 | 采集整理与确认；核心对象与关系 |
| Entry、来源证据、归档和后续修订 | 核心对象与关系；采集整理与确认 |
| 目录共创、知识空间、搜索、思维导图 | 目录与知识空间；核心对象与关系 |
| AI 阅读、项目问答、引用与知识不足 | 目录与知识空间；Agent 架构与 AI 边界 |
| PydanticAI、Provider、Agent 上下文 | Agent 架构与 AI 边界；技术与端侧边界 |
| 前端信息架构、桌面布局、小屏边界 | 目录与知识空间；技术与端侧边界；并使用 Grove UI Skill |
| 排期、Non-Goals、创建 OpenSpec change | 功能优先级与 Change 顺序；当前能力主规格 |

## OpenSpec 交接

- 新功能排期参考 [建议的 OpenSpec Change 顺序](产品蓝图/功能优先级与Change顺序.md#建议的-openspec-change-顺序)，用户当前明确决定优先；不把建议顺序当作所有开发任务的准入清单。
- 一个 change 对应独立可验收的结果或技术风险边界，遵守本次授权范围，不自动实施后续能力；Non-Goals 只列容易混入的相关能力。
- 产品专题描述目标方向；`openspec/specs/` 描述已归档能力；当前 change 描述本次行为增量；代码描述现状。
- 如果四者不一致，先判断是尚未实施的产品目标还是实际冲突；不要用旧代码反向覆盖蓝图，也不要绕过 change 静默扩张范围。
- 路线图维护方向和阶段里程碑，主规格维护已归档行为，当前 change 维护本次范围与任务状态；验收证据集中记录并引用，历史过程保留在归档与 Git，不在路线图复制完整进度台账。
- 任务分流、完整工作流、验证与批准边界以 [AGENTS.md](../AGENTS.md) 为唯一工程规则来源；产品文档不另定义一套流程。
