# 知识 Agent 任务状态修订验证

日期：2026-09-06。对应 `fix-knowledge-agent-multiturn-context`，本地分支
`codex/fix-knowledge-agent-multiturn-context`。本文记录任务状态修订，不复用首轮修复的
自动化结果代替本次验收。任务状态真实对照、原有场景回归及正文阅读复核已完成；
原生用户验收、归档、推送合并未完成。

## 实现

设计提交 `e135b50`；实现阶段提交 `cbd13e4`、`80aa7db`、`1dd6d2f`、`7c91317`，
原有场景回归修复 `8943340`、`b272637`、`a764a2d`，格式收尾 `940be85`。
保留 Pydantic AI 和现有只读工具，Run 创建时固定 `dialogue_task_v1` 协议。
上下文阶段选择任务，查询阶段读取原始消息、选定基线和项目候选并返回条件增量，
服务端完成归并、范围核实与执行；规范化查询计划标记 `task-v1`。

任务池最多五项、深入最多二层、另设字节预算并记录裁剪。自动插话保留可恢复任务，
显式重置和范围变化清空继承边界。成功结果与有效任务状态一起提交，澄清、取消和失败
不覆盖成功条件。恢复讨论只提供选定任务的合法用户陈述；工作集、项目实体和结果
序号在使用前重新核验。空列表会取代旧列表，不能继续引用旧列表中同序号的对象。
任务句柄和条件原文引用接入 Pydantic AI 输出校验，非法输出最多在模型内修正一次；
持续非法仍明确降级，修正不会替代服务端范围核实，实际请求次数保留在 usage 中。

项目候选同时来自模型提出的原文片段和服务端对当前原文的范围内名称精确匹配。
服务端最多取六个不同名称以检测五项上限，不向模型发送项目全集、Entry 内容或计数；
名称出现本身不添加过滤条件。保留未知、重名、已删除后同名替代的拒绝边界。

## 确定性验证

- 知识 Agent 后端全量相关回归：619 项通过，包含新增任务状态的 34 项测试。
- 新增覆盖条件替换/清除、深入/恢复、插话依据隔离、澄清/失败不污染状态、显式重置、
  scope_change、跨对话持久化句柄、同名项目替代、原文来源和冲突增量拒绝、固定协议、
  原始请求贯穿执行、恢复不重新规划、非法计划不回退搜索、字节预算、名称候选有界与
  不自动添加筛选、空列表引用及取消后的状态恢复、旧历史原始依据保留、任务池与深入
  上限，以及模型修正成功/耗尽重试的可观测性、可信界面项目名、性质分类原文校验和
  模型改写错误标题不能覆盖服务端实际展示对象。
  时间字段拒绝仅凭排序/数量生成区间，显式日期与相对时间范围保留支持；无法验证的
  原文先要求模型修正，超过重试预算显式失败，不静默删除条件。
- 原生端相关 API/结果适配/结果组件测试 29 项通过；typecheck、lint 通过。
- 后端变更文件 ruff、`git diff --check` 通过；OpenSpec 全库严格校验 56 项通过。
- `alembic upgrade head` 成功，本修订无新增数据库迁移。
- 真实 `/api/knowledge-agent/conversations` 未登录返回 401，端点存在。

## 真实对照设计

使用已授权 demo 账号，经登录、消息、后台 Worker 和诊断接口执行；使用后端已有
DeepSeek 文本模型配置，未读取或保存 API key。对照同一 Workspace 的当前数据：
房子装修 92、新疆旅行 10、找工作 0、云南旅行 0，总数 102。泛称知识包含全部类型；
房子装修的 knowledge 单类型仅 36 条，可检测类型误筛选，而不是只看数字是否非零。

六段任务状态对话共 21 轮，每批重复两次。保留用户五轮原话，并核对真实集合范围、
条件、输出种类、完整性及数值，预期由独立数据库查询产生。调试与保留场景在首次
真实调用前固定；保留场景首次测试暴露的问题也如实记录，后续重复属于修复回归，
不能再称为完全未见过的盲测。旧的开放讨论/指代/澄清场景另行复跑。

旧协议通过临时服务在提交 Run 时关闭任务状态；Worker 按 Run 持久化协议执行，
不会因同一时刻开关不同而改猜语义。抽查旧批 Run 无任务协议，新批 Run 均使用新协议。
自动比较验证数据、模型配置、场景摘要一致；完整运行时配置未由接口全部公开，
不能据此声称所有环境变量均由评测器证明一致。

## 保留的中间结果

| 批次 | 轮数 | 自动通过 | 失败 | 阅读复核 | 说明 |
|---|---:|---:|---:|---:|---|
| `20260906T052833Z-1be58d` | 42 | 24 | 16 | 2 | 旧协议对照 |
| `20260906T052722Z-4185d2` | 5 | 0 | 5 | 0 | 新协议初测：泛称类型误筛选和路由错误 |
| `20260906T053015Z-090148` | 9 | 9 | 0 | 0 | 调试集局部复测，不能代表全套稳定 |
| `20260906T054027Z-d6cb4d` | 42 | 30 | 10 | 2 | 完整复测暴露项目候选遗漏、旧查询指令干扰 |
| `20260906T054636Z-d06da2` | 42 | 39 | 1 | 2 | 条件统计已修正，剩余非法任务句柄被转为澄清 |

旧协议整段严格通过 3/12，无多余澄清；151 条模型阶段记录，80 条有 usage。
首轮完整新协议整段严格通过仍为 3/12，多余澄清 2 次，不能仅凭通过轮数增加就说
连续任务已经解决。上述失败推动了 `1dd6d2f` 的通用输入和实体候选修复。
旧对照与早期调试有并行运行、开发服务重载，耗时不能作为严格的性能提升证明。
第二次完整复测仍有一轮失败（Run 492），不得从报告移除；该缺陷推动了
`7c91317` 的模型内引用校验修正。两段通用回答已阅读，均未查询 Grove。

原始 JSON/Markdown 放在 Git 忽略的 `data/knowledge-agent-evals/`，没有改评分规则或
把失败轮改成歧义来提高成绩；正常列表 `limited` 仍按既有严格评分计为 fail，须另行
核对对象、顺序、范围和工具状态。开放讨论的 review 必须阅读实际回答。

## 最终结果

被测提交 `7c91317`，批次 `20260906T055436Z-32f66c`，北京时间 13:54:36–13:58:29。
Conversation 212–223、Run 511–552，共 12 段对话、42 轮，整批正常完成，九张业务表
哈希逐轮和结束时保持不变。数据、模型、场景的机器对照检查通过。

| 指标 | 旧协议 | 最终任务状态版本 |
|---|---:|---:|
| 实际轮数 | 42 | 42 |
| 严格自动通过 | 24 | 40 |
| 严格自动失败 | 16 | 0 |
| 正文阅读复核 | 2 | 2 |
| 多余澄清 | 0 | 0 |
| 整段严格自动通过 | 3/12 | 10/12 |
| 模型阶段记录 | 151 | 126 |
| 最终模型降级阶段 | 0 | 0 |
| 单轮中位数/最大值（秒） | 6.49/22.32 | 5.06/20.29 |

最终两轮 review 为 Run 524、545，已阅读实际回答和依据：均正常解释番茄工作法，
未查询 Grove；后续 Run 525、546 均恢复房子装修方法统计并按信息性质分组，没有
继承讨论任务的禁止查询限制。正文复核纳入后 12/12 段完成本次任务要求，原始报告
仍保留 2 个 review，不改写成自动通过，也不把百科内容自然度当成已全面验收。

用户五轮原话两次均通过。最后 Run 515、536 的集合为完整 Workspace、无项目/主题/
类型附加条件，项目桶为房子装修 92、找工作 0、新疆旅行 10、云南旅行 0。
保留当前项目的类型分组、明确返回全局、类型替换与撤销、显式重置也均通过。
原先划分的两个保留场景在最终重跑中共 12/12 轮通过；由于首测已经用于发现问题，
此结果是冻结场景的修复回归，不作为独立未见盲测泛化率。

126 条记录都是 `llm/deepseek-chat`；82 条有 usage，累计已记录输入 175674 token、
输出 11736 token、83 次实际请求，其中一个查询阶段在既有预算内修正一次后成功。
剩余阶段无 usage，不能据此估算完整 token、请求量或费用。阶段数不等于包含 SDK
重试的实际请求总数。旧批存在并行运行条件，延迟只报告观测值，不证明严格加速。

- [旧协议对照](../../data/knowledge-agent-evals/task-state-baseline/20260906T052833Z-1be58d/report.md)
- [最终逐轮报告](../../data/knowledge-agent-evals/task-state-final/20260906T055436Z-32f66c/report.md)
- [最终原始响应、条件和诊断](../../data/knowledge-agent-evals/task-state-final/20260906T055436Z-32f66c/report.json)

## 原有场景回归

第一批 `20260906T055914Z-480b79`：14 pass、8 fail、1 blocked、4 review，业务哈希不变。
暴露可信界面项目名被误判为无来源，以及正式记录偶发被附加 fact 性质筛选。
`8943340` 接受可信界面名称来源，并对性质分类增加原文词项校验和模型内修正。
该校验只说明原文存在对应分类，不证明完整语义正确，也不把所有自然语言理解
替换为词项规则；分类全集和语义仍由现有 schema 与模型控制。

第二批 `20260906T060351Z-68a4a8`：20 pass、3 fail、4 review，业务哈希不变。
项目范围、空项目及性质误筛选问题已修复，但发现项目分组被误认为必须提供项目
候选全集、列表排序被附加时间条件，以及真实第二条 Entry 81 被模型摘要改写为
第一条标题。这些都算本次验收缺陷，未从评分移除。

`b272637` 明确名称候选不是项目全集、聚合工具返回零条桶，排序和数量不生成时间
筛选；展示对象提供显式序号，服务端用实际快照固定序号对应标题，历史模型摘要
不得覆盖真实对象身份。所有 Entry/Source 仍在当前 Run 复验后才作为证据。
第三批 `20260906T060818Z-d74ba6`：20 pass、2 fail、5 review，业务哈希不变。
项目聚合和对象引用已修正：Run 618 精确解释第二条 Entry 81「镜柜边预留插座」，
引用本轮 Evidence 753、Source 73 的已核验原文。开放讨论 Run 620/622、626/628
均已阅读，连续承接且 Grove used=false。失败保留为 Run 617 的无请求时间上限
（对象及顺序正确，但不能忽略错误条件），以及 Run 640 的模型引用修正耗尽。
`a764a2d` 增加时间字段原文校验，并向模型返回具体无效来源和原文以改正引用。

交叉批次 `20260906T060918Z-5f41ac`，被测 `940be85`：21 轮中 20 pass、1 review、
0 fail、0 多余澄清。开放回答 Run 645 已阅读通过，统计原话和替换/撤销/恢复再次通过。
63 条模型阶段、0 最终降级；与原有回归并行，耗时受排队影响，不作性能比较。
该批在最后的时间校验之前完成，不能冒充最后提交的全量任务集重测。

最终提交 `a764a2d`，批次 `20260906T061353Z-0f2252`：27 轮正常完成，21 pass、
1 fail、5 review，无多余澄清，81 条模型阶段均为真实 DeepSeek、0 模型 fallback。
单轮中位数 5.08 秒、最大 10.13 秒；业务表哈希保持不变。严格评分原样保留。

唯一 fail 为列表 Run 665：独立 SQL 预期和实际均为 82、81、80、79、78，方法类型、
房子装修范围、倒序与数量均正确，没有额外时间筛选。集合完整性 complete，列表
完整性 limited；只有 `tool:query_entries` 的正常限五条被既有摘要标为降级。
此项属于此前已知的截断标记问题，不伪装成模型失败或统计口径错误，也不修改本次评分。

5 个 review 已逐条阅读：Run 666 解释真实第二条 Entry 81，并引用本轮 Evidence 754、
Source 73「13、镜柜边\n可以用来插吹风机，电热毛巾架」；Run 667/668、670/671
完成通用讨论、压缩三个步骤和适用任务追问，均 Grove used=false。所有 12 段对话
均完成本次规定的连续任务，设备显示与文风/百科准确性的全面验收仍不由本记录代替。

- [最终原有场景逐轮报告](../../data/knowledge-agent-evals/task-state-regression/20260906T061353Z-0f2252/report.md)
- [原有场景原始响应和诊断](../../data/knowledge-agent-evals/task-state-regression/20260906T061353Z-0f2252/report.json)
- [任务状态交叉回归](../../data/knowledge-agent-evals/task-state-final/20260906T060918Z-5f41ac/report.md)

本轮统计、继承、引用与来源校验已完成实施和自动化验证，仍可能出现模型未正确理解
自然语言且重试耗尽的情况；这些会显式可见，不承诺任意表达零错误。上述批次的失败
及修复轨迹完整保留。最终时间来源校验是在交叉批之后增加，已覆盖确定性测试和
原有场景全量复跑；未再以相同 task-suite 做重复费用扩大的全套采样。

## 待用户原生走查

1. 新建全部知识对话，按原话连续问：全部项目我一共有多少个知识 → 其中房子装修项目有多少个呢 → 按项目统计数量给我 → 帮我统计新疆旅行有多少条知识 → 分项目统计数量。当前数据预期总数 102，项目桶 92/0/10/0。
2. 先统计房子装修的方法，再换为参数，撤销类型限制，再撤销项目限制；检查实际口径和数值依次变化。
3. 插入一次“不查知识库”的通用讨论，再回到先前统计；检查讨论依据与统计范围没有互相污染。
4. 列出最近五条方法，再解释第二条并打开原文；确认解释对象与实际第二张卡一致。
5. 切换项目范围或显式新话题，检查旧条件不会带入；观察项目名称、类型、零条项目和统计摘要展示。

自动化与正文阅读不代替设备交互、视觉或读屏验收。等待用户反馈通过后，再展示规格
同步差异并取得归档批准；不提前推送或合并。
