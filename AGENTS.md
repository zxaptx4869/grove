# 知林 Grove — 代理工作守则（AGENTS.md）

本文件是仓库内 AI 代理（Codex 等）与协作者共同遵守的工作守则，随 OpenSpec 工作流一并维护。

## 1. 产品守则（铁律）

1. **AI 输出永远是候选。** AI 生成的任何抽取、归类、建议、回答，都只能作为候选（Extraction / 建议），不得直接写入或覆盖正式记录（Entry）。
2. **正式记录必须可溯源。** 每条正式 Entry 必须能追溯到其 Source（原始材料）；丢失来源的记录视为缺陷。
3. **数据按 Workspace 隔离。** 用户数据从第一行代码起按 Workspace 隔离，任何跨 Workspace 的读写都是缺陷。
4. **人在环上。** 人的确认/拒绝/修改是产品闭环的一部分，任何自动化不得绕过人的最终决定权。

本节约束 Grove 产品内的数据与 AI 行为，不新增开发过程的逐项审批要求。开发代理按用户授权与下述工程工作流执行；正式记录的人工确认、来源追溯与 Workspace 隔离边界保持不变。

## 2. 工程工作流（OpenSpec）

- **任务分流**：服务或模拟器启停、环境排查、纯文档与配置维护，以及恢复既有规格行为的小 bug，直接处理，不要求新建 change 或例行加载 OpenSpec Skill。需要查明预期行为时只读相关规格。新增能力或改变产品行为、接口与数据合同、权限、AI 工具及执行边界时建立 change；按实际影响判断，不按代码行数或“修复”名称判断。提示词调整也遵循此边界。
- **正式变更流程**：`proposal → specs/design → tasks → 前置校验 → 实施与相关验证 → 适用的手动验收 → 同步规格并归档 → 收尾提交`。先创建 `openspec/changes/<name>/`，四类工件完整表达必要信息，按依赖顺序生成，`openspec validate --all --strict` 通过后再动业务代码。一个 change 对应独立可验收的结果或技术风险边界，不按每个内部技术步骤机械拆分。
- **工件与读取**：四类工件均保留，篇幅与复杂度相称；proposal 写原因和范围，specs 写可验证的行为，design 写必要技术决策，tasks 写工作项与验收入口。复用引用，不为填满模板重复背景。首次接手建立范围和关键约束，随后按当前任务读取相关段落；复用已读且未变化的内容，发现关联影响或冲突再扩大读取，不因阶段切换重读全部工件。
- **自主执行与澄清**：在已授权范围内持续推进，普通错误先定位、修复并验证。只有缺少必要信息、需要新增授权，或现有证据不能解决实质性产品分歧时，暂停依赖该决定的工作，其他工作继续。用户只要求审阅、提案或探索时遵守该范围；已要求完整实施时，不因 Skill 阶段结束而要求再发“继续”。
- **批准的范围与复用**：本任务中已明确的 change、决定和批准持续有效，不逐文件或逐阶段重复询问。修改已有计划前，集中展示跨工件的范围、差异和理由，经用户整体或部分批准后执行；新增超出批准范围的计划修改另行确认。任务勾选和真实验证结果记录按已有实施授权执行，不视为重新审批计划。缺失授权不得由等待时间或默认选项替代。
- **验证范围**：根据受影响的后端、Web、原生端或扩展执行相关测试和静态检查；共享接口与核心边界变化覆盖相关调用方。正式 change 实施前与收尾时均须确认全库 OpenSpec 严格校验通过；普通工件编辑按影响校验，已通过且相关输入未变的检查不机械重复。不能以工件存在或任务已勾选替代实际验收。
- **同步与归档**：实施、验证和适用的手动验收全部完成后，集中展示归档目标、规格同步差异及异常项，取得覆盖本次操作的批准后执行 `openspec archive <change>`，核验主规格与归档结果；已有批准覆盖目标和差异时直接沿用。常规收尾不另做一轮独立 sync、不强制委派代理或直接移动目录。用户取消即停止归档；未完成验收或未同步不得报告正常完成。未归档的 change 不算全部完成，归档位于收尾提交和推送合并之前。
- **推送与合并必须经用户确认**：实现后停留在本地分支；用户验证无问题并明确批准后，才推送或合并。批准本地实现、提交或归档不等于批准推送合并。
- **本地提交及时化**：每完成一段可验证的修改（功能、修复、测试、文档或配置），立即进行本地阶段提交。归档后再提交收尾变更。等待人工验收时先保存已验证成果，报告“实现与自动化验证完成，待人工验收”，不得提前归档或宣称 change 全部完成。
- **原生端手动验收职责**：AI 默认运行原生端自动化检查（测试、typecheck、lint），提供走查清单；iOS/Android 模拟器及真机的手动交互、视觉、安全区、系统键盘和读屏验收默认由用户执行并反馈。除非用户明确要求，AI 不安装、启动或操作模拟器/真机，也不生成代替用户的手动验收结论。用户明确要求启动或排查设备时，可执行该操作，但不等于已通过产品验收。需要原生端手动验收的 change，用户反馈通过后方可视为走查完成。
- **分支策略**：正式功能 change 自 proposal 阶段起新建 `codex/<change>` 特性分支，不在 main 上直接开发；小 bug 修复、文档与配置维护不强制新分支。
- **收尾遗留登记**：收尾时区分本次验收缺陷与范围外优化。前者按当前任务处理；后者逐项说明背景、原因与影响，集中询问是否登记。用户同意后才写入 `docs/discussions/Grove后续优化清单.md`，由代理选择分组并注明来源任务或 change 与日期。等待登记决定不阻塞已满足条件的本地提交或归档；未获同意不写入清单。
- CLI 可用命令：`openspec list`、`openspec status --change <name>`、`openspec instructions <artifact> --change <name>`、`openspec validate --all --strict`、`openspec archive <change>`。

## 3. 工程约定

- 技术栈（2026-08-12 锁定）：后端 FastAPI + Pydantic v2 + async SQLAlchemy 2 + Alembic；前端 React 19 + TypeScript + Vite + Tailwind 4 + shadcn/ui + TanStack Query；数据库开发 SQLite、生产 MySQL 8。
- 后端 Python ≥3.12；依赖管理用 backend/pyproject.toml；格式与静态检查用 ruff。
- 环境变量统一走 pydantic-settings，密钥类配置只放占位键，`backend/.env.example` 提供模板，不得提交 `.env`。
- 开发环境后端以 `cd backend && .venv/bin/alembic upgrade head && .venv/bin/uvicorn app.main:app --reload` 启动（`alembic upgrade head` 幂等，已最新时零操作；改代码自动重载）；凡涉及新增或修改 Alembic 迁移的代码改动，`--reload` 不会自动应用迁移，改完必须手动再执行一次 `cd backend && .venv/bin/alembic upgrade head` 再验证；改 `.env` 或安装依赖后仍需手动重启；新增或修改后端端点后先 `curl` 验证（预期 401/200，而非 404）再进入走查；生产环境不使用 `--reload`，生产迁移按变更窗口与回滚策略单独执行。
- Grove Web 是桌面知识整理工作台，从 1024px 视口宽度开始支持完整业务流程；低于该宽度时只显示电脑访问提示，不实现手机 Web 业务界面。
- 提交信息用中文、遵循 Conventional Commits 风格（feat/fix/docs/chore/refactor）。
- 前端服务端状态查询默认即时（TanStack Query `staleTime: 0`，在 `main.tsx` 全局默认）；只有明确静态且昂贵的查询才在各自 `useQuery` 显式设置更长 `staleTime`，并注明缓存理由。后台 Worker 或跨页面变更的数据不得依赖长缓存。
- 产品范围与优先级相关任务从 `docs/产品蓝图.md` 路由到相关专题，通常读取 1 至 2 份，不默认加载全部；环境操作和普通排查不例行读产品文档。用户明确的新决定不能被过时材料覆盖；跨 change 的实质性产品分歧先解决并同步权威专题，技术取舍记入当前 change 的 `design.md`。没有 change 的小修复不为记录说明强造工件。
- **AI 可观测性（防静默降级）**：所有 AI 生成路径必须记录 provider / model / fallback 状态；接口返回成功不等于真实模型调用成功，禁止静默降级。发生降级时必须日志告警，并在响应或界面中可识别。

## 4. 代码注释与沟通语言

- 仓库内代码注释、文档、提交信息一律使用中文。
- 与用户沟通使用中文。
- **术语约定**：用户所说的「旧项目」指 KnowStruct（`/Users/hujun/Documents/软件/trae/KnowStruct`），Grove 的前身产品。
