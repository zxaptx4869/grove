{
  "name": "grove-browser-capture",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Grove 快采：浏览器框选截图，一键发送到收集箱",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0",
    "@types/chrome": "^0.0.268",
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
