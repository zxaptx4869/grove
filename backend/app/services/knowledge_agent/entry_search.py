"""有界正式 Entry 查找服务（任务组 4 完整实现）。

本模块由结果形态路由的 entries 分支调用；当前仅作为接线桩，
保证执行图与测试在路由阶段可独立验证。
"""


async def execute_structured_entry_search(db, run, decision, ctx):
    """执行有界结构化 Entry 查找（由后续任务实现）。"""
    raise NotImplementedError("结构化 Entry 查找由任务组 4 实现")
